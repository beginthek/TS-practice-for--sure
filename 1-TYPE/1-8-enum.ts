
{
    /**
     * Enum
     */
    //JavaScript

    const MAX_NUM = 7;
    const MAX_STUDENTS_PER_CLASS =3;
    //각 각 독립적인 상수
    const MONDAY = 1;
    const WENNESDAY = 2;
    const DAYS_ENUM = Object.freeze({"MONDAY":0, "TUESDAY":1});
    const dayOfToday = DAYS_ENUM.MONDAY 
    
    enum Days{
        Monday,
        sasd,
        adfaf,
        adfadf,
        asdaf,
        Sunday,
    }
    console.log(Days.Monday);
    const Day = Days.Sunday;
    console.log(Days);
}
 